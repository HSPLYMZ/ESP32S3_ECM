# ESP32S3_ECM_V1 功能文档 V1.2

## 1. 项目概述

- 当前目标：实现 EC200A ECM 模式 4G 转 Wi-Fi 最小路由器
- 硬件平台：ESP32-S3 + EC200A-CN
- 版本：V1.2

## 2. 已实现模块

### 2.1 EC200A ECM 上行

- USB Host CDC 驱动安装，自动探测 EC200A
- AT 口打开、AT 握手
- ECM 模式配置、APN 下发
- ECM 数据会话建立、IP 获取、默认路由设置
- USB 热拔插与自动重建链路
- 链路断开/拨号失败自动重试

### 2.2 Wi-Fi SoftAP

- 802.11 b/g/n SoftAP，WPA2 加密
- 默认 SSID EC200A，最多 2 个 STA
- DHCP Server 分配 192.168.4.x 网段 IP
- 上游 DNS 同步到 SoftAP DHCP 下发

### 2.3 NAPT 网络转发

- ECM IP 获取后自动启用 NAPT
- 客户端通过 SoftAP 可透明访问公网
- 实测性能：下行 4.85 Mbps，上行 5.05 Mbps，时延 74 ms

### 2.4 配置管理

- NVS 持久化：SSID、密码、信道、APN
- 参数校验与默认值填充
- 支持移动/联通/电信/自定义 APN

### 2.5 电源与温控管理

- 内部温度传感器采样（每 5s）
- 50C 预警，>=60C 持续 60s 触发 light sleep 保护
- 恢复阈值 55C
- 无客户端 + ECM 稳定 30s 后空闲 light sleep
- 30s 定时唤醒并自动恢复 SoftAP + ECM + NAT

### 2.6 运行状态跟踪

- 线程安全共享状态（互斥锁保护）
- SoftAP 状态、客户端数、温度、休眠/温控标志

## 3. 文件结构

```
main/
  main.c              # 启动入口
  app_config.c/.h     # NVS 配置管理
  app_state.c/.h      # 线程安全共享状态
  app_tasks.h         # 任务优先级与 Core 分配
  cellular_ecm.c/.h   # EC200A ECM 上行、AT、NAPT、重连
  wifi_ap.c/.h        # Wi-Fi SoftAP、DNS 下发
  power_manager.c/.h  # 温控与休眠管理
```

## 4. 启动流程

1. NVS 初始化，加载配置
2. Wi-Fi 栈初始化
3. 启动 SoftAP
4. 启动 ECM 管理链路（USB CDC 驱动 -> 探测 EC200A -> AT 握手 -> ECM 模式配置 -> 等待 IP）
5. 启用 NAPT
6. 启动温控与休眠管理

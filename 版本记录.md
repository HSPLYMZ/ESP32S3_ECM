# ESP32S3_ECM 版本记录

## V1.3.0 - 2026-06-17

- 清理 `cellular_ecm.c` 中废弃的休眠/挂起接口和不可达 `if (false)` 分支，降低 ECM 状态机复杂度。
- 优化 ECM 重连架构：断链后优先进行 ECM 上行恢复，连续失败时记录故障并触发主机级恢复。
- 新增 ECM 诊断持久化：保存重连次数、失败次数、最近失败原因、CGATT 附着状态。
- 联网诊断从单一目标升级为多目标检测：`223.5.5.5:53`、`180.76.76.76:53`、`114.114.114.114:53`。
- 修正热点 DNS 策略，固定向客户端下发公共 DNS，减少运营商私有 DNS 或单点 DNS 异常导致的“连上热点但不能上网”。
- 移除 Wi-Fi 初始化中不必要的 `APSTA -> AP` 模式切换，关闭 Wi-Fi 省电并提升 SoftAP 长连稳定性。
- 新增 NVS 本地故障环形日志，记录启动、ECM、诊断、WebUI 等关键故障，可通过 `/api/faults` 查询。
- 增强 WebUI 可靠性：非阻塞 accept、读写超时、WebUI 监控任务和故障限频记录。
- WebUI 改为 iOS 风格控制中心界面，新增页脚作者邮箱和 GitHub 地址。
- 非核心服务启动失败时改为记录日志和故障，不再直接重启整个路由器。
- 工程版本统一到 `V1.3.0`，同步更新 `PROJECT_VER`、README、功能文档和记忆文档。

## V1.2.5 - 2026-06-17

- 移除旧休眠路径，降低 ECM 链路恢复时的状态冲突风险。
- 新增温控 LED 辅助反馈。
- 增强 AT 命令重试逻辑。
- 将 CGATT 等待窗口调整为 45 秒，提升弱网注册成功率。
- 加入 TWDT 看门狗保护，减少任务卡死后无响应的风险。
- 持续清理代码结构，为后续断网诊断和自恢复能力做准备。

## V1.2.4 - 2026-06-12

- 修复 ECM suspend/teardown 顺序：销毁 ECM 栈前先解绑输入路径并释放 netif glue，避免 light sleep 前后的崩溃。
- 修复 SoftAP DHCP DNS 下发选项，统一使用 DNS offer 标志，减少热点已连接但 DNS 异常的边缘问题。
- 修复 `cellular_ecm_start()` 中重复创建状态互斥锁的问题，并补齐 ECM 创建失败时的回滚清理。
- 清理 `main/` 下的乱码和不必要中文说明，统一为简洁英文头注释、状态文案和错误提示。
- 统一版本号到 `V1.2.4`，同步更新 `PROJECT_VER`、README 和记忆文档。

## V1.2.3 - 2026-06-12

- 代码结构优化：`cellular_ecm.c` 内部按功能域分区注释。
- 锁机制统一：`portMUX` 自旋锁替换为 `SemaphoreHandle_t` mutex。
- OTA 准备：分区表切换为 `TWO_OTA_LARGE`。
- 启动信息增强：打印完整固件 banner。
- 修复 `s_status_mutex` 初始化顺序，避免回调空指针。

## V1.2.2 - 2026-06-12

- `update_radio_status_snapshot()` 栈缓冲区改为 static 复用。
- `app_state` 新增细粒度 getter，避免全量快照拷贝。
- lwIP TCP/IP 任务显式绑定 Core 1。
- 双核利用率优化：Core 0 专注 Wi-Fi/USB/ECM，Core 1 承载 lwIP 和 power manager。

## V1.2.1 - 2026-06-12

- AT 轮询从 500 ms 降至 5 s，降低 CPU 和串口压力。
- suspend 流程改为任务通知唤醒，降低 WDT 风险。
- 启用板载 8 MB PSRAM。
- 编译优化从 `-Og` 切换为 `-Os`。
- 显式定义关键任务栈大小宏。

## V1.2 - 2026-06-12

- ECM 最小闭环稳定运行，客户端可通过 NAPT 透明上网。
- 新增内部温度采样、温控保护和空闲休眠。
- 实测性能：下行 4.85 Mbps，上行 5.05 Mbps，时延 74 ms。

## V1.1 - 2026-06-10

- 启动路径收敛为 SoftAP + `cellular_ecm`。
- 删除 `webui`、`led_status`、`system_monitor` 等非必要模块。
- 板端验证通过：AT ready、ECM connected、ETH got IP、NAPT enabled。

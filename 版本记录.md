# ESP32S3_ECM_V1 版本记录

## V1.2.3 - 2026-06-12

- 代码架构优化：cellular_ecm.c 内部按功能域分区注释（Helpers / AT Layer / ECM Driver / State Machine / Public API）
- 锁机制统一：portMUX 自旋锁替换为 FreeRTOS SemaphoreHandle_t mutex，与 app_state 层一致
- OTA 准备：分区表切换为 TWO_OTA_LARGE（factory + ota_0 + ota_1），支持双 OTA 分区
- 启动信息增强：打印完整固件 banner（版本、芯片、模组、IDF 版本）
- 修复 s_status_mutex 初始化顺序：移到 cellular_ecm_start 函数开头，避免回调空指针

## V1.2.2 - 2026-06-12

- 内存管理优化：update_radio_status_snapshot 栈缓冲区改为 static 复用（节省 640B 栈压力）
- app_state 新增细粒度 getter（is_softap_started / get_sta_count），避免 200B 全量快照拷贝
- lwIP TCPIP 任务显式绑定 Core 1，分散网络栈负载
- 双核利用率优化：Core 0 专注 Wi-Fi/USB/ECM，Core 1 承载 lwIP + power_manager

## V1.2.1 - 2026-06-12

- FreeRTOS 调度优化：稳定态 AT 轮询从 500ms 降至 5s（APP_AT_STABLE_POLL_MS）
- Suspend 安全加固：enter_suspend_state 用任务通知替代 busy-wait，消除 WDT 风险
- 启用 PSRAM：ESP32-S3 板载 8MB Octal PSRAM 加入堆分配器
- 编译优化：-Og 切换为 -Os，固件体积缩减约 20%
- 显式任务栈大小宏：APP_TASK_STACK_CELLULAR_MANAGER / APP_TASK_STACK_POWER_MANAGER

## V1.2 - 2026-06-12

- ECM 最小闭环稳定运行，客户端可通过 NAPT 透明上网
- 新增内部温度传感器（每 5s 采样）、温控保护、空闲休眠
- 实测性能：下行 4.85 Mbps，上行 5.05 Mbps，时延 74 ms

## V1.1 - 2026-06-10

- 启动路径收敛为 SoftAP + cellular_ecm
- 删除 webui、led_status、system_monitor 等非必要模块
- 板端验证通过：AT ready, ECM connected, ETH got IP, NAPT enabled, path is up
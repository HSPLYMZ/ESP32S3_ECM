# ESP32S3_ECM 功能文档 V1.3.0

## 1. 系统定位

ESP32S3_ECM 是一个基于 ESP32-S3 与 EC200A-CN 的 4G 转 Wi-Fi 路由工程。EC200A 通过 USB ECM 提供蜂窝上行，ESP32-S3 提供 SoftAP 热点、DHCP、NAPT、联网诊断、本地故障日志和 WebUI 控制台。

## 2. 核心数据链路

1. ESP32-S3 启动 USB Host。
2. 识别 EC200A 的 ECM control/data 接口和 AT 接口。
3. 通过 AT 命令配置 ECM 模式、APN 和蜂窝附着。
4. ECM 网卡获取蜂窝侧 IP。
5. SoftAP 启动并向客户端下发 IP、网关和公共 DNS。
6. ESP32-S3 启用 NAPT，将热点客户端流量转发到 ECM 上行。

## 3. Wi-Fi 热点

- 默认热点名称：`EC200A`
- 工作模式：SoftAP
- DHCP：ESP32-S3 本地分配热点网段地址
- DNS：固定下发公共 DNS，避免模组侧私有 DNS 异常影响客户端
- 稳定性策略：关闭 Wi-Fi 省电、使用 HT20 带宽、提升客户端 inactive timeout

## 4. ECM 自动恢复

- USB 断开时记录故障并等待设备重新枚举。
- ECM link down 时请求链路重建。
- 联网诊断连续失败后触发 ECM 重连。
- 连续多次 ECM bringup 失败时记录最近失败原因，并执行主机级恢复。
- 重连次数、失败次数、CGATT 和最近失败原因会保存到 NVS，便于断电后继续定位。

## 5. 联网诊断

联网诊断任务周期性运行，默认约 30 秒探测一次。探测目标包括：

- `223.5.5.5:53`
- `180.76.76.76:53`
- `114.114.114.114:53`

只依赖单一目标容易被运营商策略、区域 DNS 或防火墙误导；V1.3.0 改为多目标后，误判概率明显降低。连续失败达到阈值后，诊断任务不会立即重启整机，而是先请求 ECM 重连。

## 6. 本地故障环形日志

V1.3.0 新增 `fault_log` 模块：

- 使用 NVS 保存最近故障记录。
- 记录 boot、ECM、diag、webui 等来源。
- 支持 `/api/faults` 读取 JSON 日志。
- WebUI 异常采用限频记录，避免短时间刷爆 NVS。

## 7. WebUI

- 提供状态总览、上行信息、诊断结果、故障日志和配置展示。
- V1.3.0 更新为 iOS 风格控制中心界面。
- HTTP 服务使用非阻塞监听 socket。
- 客户端读写设置超时，减少半开连接卡住 WebUI 的概率。
- 监控任务会检测 WebUI 长时间无响应并关闭 socket 触发自恢复。
- 页面页脚包含作者邮箱 `hsp2028.163.com` 和 GitHub 地址 `HSPLYMZ/ESP32S3_ECM`。

## 8. FreeRTOS 任务职责

- `main`：启动编排，初始化 NVS、状态、Wi-Fi、ECM、诊断、WebUI 等模块。
- ECM 任务：负责 EC200A 枚举、AT 控制、ECM bringup 和链路恢复。
- Wi-Fi/AP 相关任务：由 ESP-IDF Wi-Fi 栈负责 SoftAP 接入、DHCP 和事件回调。
- 诊断任务：周期性检测公网连通性，失败时请求 ECM 重连。
- WebUI 任务：处理 HTTP 请求和 API 输出。
- WebUI monitor 任务：监控 WebUI 卡死/半开连接并辅助恢复。
- Power manager 任务：负责温度/电源辅助监控。

## 9. 非核心功能容错

V1.3.0 将 WebUI、诊断、LED、电源管理等非核心功能从硬失败改为软失败：启动失败时记录日志和故障，但不使用 `ESP_ERROR_CHECK` 直接重启整机。这样可以保证路由主链路优先存活。

## 10. 建议测试项

- COM25 冷启动日志检查。
- 热点客户端长时间上网测试。
- 多客户端同时接入压力测试。
- 运营商弱网、欠费、无 SIM、CGATT 失败场景测试。
- EC200A USB 拔插与供电抖动测试。
- WebUI 多次刷新、半开连接和手机浏览器后台切换测试。
